
# Bed build intermediates. Regenerated by build-reader-beds.py; 15MB of WAV
# has no business in a repo that ships mp3.
_reader-bed-tmp/

# Function deps — installed by npm, not committed.
functions/node_modules/
